@{
    ViewData["Title"] = "Yeni Ürün Ekle";
    Layout = "_Layout";
}


<div class="max-w-3xl mx-auto">
    <div class="bg-white rounded-xl shadow-md overflow-hidden">
        <div class="px-6 py-4 bg-gradient-to-r from-blue-600 to-blue-700 text-white">
            <h2 class="text-2xl font-bold flex items-center">
                <i class="fas fa-plus-circle mr-3"></i>Yeni Ürün Ekle
            </h2>
            <p class="text-blue-100 mt-1">Depoya yeni ürün eklemek için formu doldurun</p>
        </div>

        <div class="p-6">
            @if (ViewBag.Error != null)
            {
                <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded" role="alert">
                    <div class="flex items-center">
                        <i class="fas fa-exclamation-circle mr-3"></i>
                        <p>@ViewBag.Error</p>
                    </div>
                </div>
            }

            <form method="post" class="space-y-6">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-tag mr-2 text-gray-500"></i>Ürün Adı
                    </label>
                    <input name="name" type="text" required
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200"
                           placeholder="Örn: Laptop, Telefon..." />
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-align-left mr-2 text-gray-500"></i>Açıklama
                    </label>
                    <textarea name="description" rows="3"
                              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200"
                              placeholder="Ürün hakkında detaylı bilgi..."></textarea>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-cubes mr-2 text-gray-500"></i>Başlangıç Stok Miktarı
                    </label>
                    <input name="initialStock" type="number" min="0" required
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200"
                           placeholder="0" />
                </div>

                <div class="bg-gray-50 p-4 rounded-lg">
                    <label class="flex items-center cursor-pointer">
                        <input type="checkbox" name="isAllowed" value="true" checked
                               class="w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500" />
                        <span class="ml-3 text-sm font-medium text-gray-700">
                            <i class="fas fa-truck mr-2 text-green-600"></i>Sevkiyat İzni Ver
                        </span>
                    </label>
                    <p class="ml-8 mt-1 text-xs text-gray-500">Bu ürün için sevkiyat işlemleri yapılabilsin</p>
                </div>

                <div class="flex items-center justify-between pt-4 border-t">
                    <a asp-controller="Home" asp-action="Index"
                       class="text-gray-600 hover:text-gray-800 font-medium transition duration-200">
                        <i class="fas fa-arrow-left mr-2"></i>Geri Dön
                    </a>
                    <button type="submit"
                            class="bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-8 py-3 rounded-lg font-medium transition duration-200 shadow-lg hover:shadow-xl">
                        <i class="fas fa-save mr-2"></i>Kaydet
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>